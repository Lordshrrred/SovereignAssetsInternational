{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "git add -A && git diff --cached --quiet || (git commit -m \"auto: update site\" && git push origin main && vercel --prod --yes)",
            "timeout": 180,
            "statusMessage": "Deploying to Vercel..."
          }
        ]
      }
    ]
  }
}
